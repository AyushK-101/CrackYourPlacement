int maxProfit(vector<int>& prices) {
         int profit=0,dip=prices[0];

         for(int i=1;i<prices.size();i++){
            dip = min(prices[i],dip);
            profit = max(profit,prices[i]-dip);
         }
         return profit;
}
